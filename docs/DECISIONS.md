# سجل القرارات — المستشار الزراعي

> كل قرار يتخذه المستخدم يُكتب هنا بتاريخه. أحدث في الأعلى. القرارات بلا تاريخ مستخدم = افتراض محافظ وثّقه الوكيل.

## 2026-09-26 — قرارات جولة إصلاح الانتكاسات (أ/ب/ج/د)

| # | القرار | التفصيل | السبب |
|---|---|---|---|
| R1 | فلتر وضع المزارع يبقى على البحث اليدوي حصرًا؛ عرض المسح (#scanResults) يعرض القائمة الكاملة في الوضعين | أُدخل isScanView في render(): البحث اليدوي في المزارع يبقى مفلترًا (المواصفة ج1)، ونتائج المسح لا تُفلتر أبدًا | فلتر f1058dd كان يُطبَّق على كل استدعاءات render فأخفى نتائج EPA/EU الممسوحة عن المزارع وأبدى الأتمتة معطلة؛ إخفاء نتيجة مطابقة من صورة نجحت شكل خطر سلامة |
| R2 | التصنيف الوظيفي (cat chips) خارج شرط showDetails في الوضعين | سطر واحد: x.r.category بلا showDetails؛ raw/matchType تبقى للمحترف حصرًا | المواصفة تلزم التصنيف في الوضعين بلا استثناء؛ كان محصورًا بالشرط منذ cf2ff79 |
| R3 | تبديل الوضع يعيد التصيير وفق حالة النتائج المحفوظة لا وفق ظهور بطاقة في DOM | شرط modeSel change صار على lastResults/lastScanResults لا على #results .result | عندما تُخفى كل بطاقات المزارع بالفلتر كان التبديل للمحترف لا يُظهر شيئًا |
| R4 | تصدير التشخيص: HTML عربي RTL بعناوين t() ×4 قواميس بدل JSON خام | كل حقل بعنوان عربي؛ لا مفاتيح برمجية؛ يبقى تنزيلًا يدويًا محليًا | مطلب برومبت الجولة؛ وانضباطًا لقاعدة no-hardcoded-arabic كل النصوص عبر i18n |
| R5 | إشعارات المشاركة في dbBanner المرئي في كل الشاشات (وليس #ocrMsg المخفية خارج شاشة المسح) | notify() تكتب في dbBanner مع مؤقت اختفاء؛ ocrMsg بديل احتياطي | هذا هو سبب «الزر صامت» الفعلي: الإشعارات كانت تُرسم في عنصر مخفي |
| R6 | لا فشل صامت في مسارات المشاركة: استثناء الحافظة يُذكر داخل إشعار النتيجة النهائية | clipboardErr يُحفظ ويُدرَج بين قوسين في إشعار saved/fail | إشعار الفشل العابر كان يُستبدل فورًا بإشعار vCard — اكتشفه اختبار التتبع الفعلي (د: trace 3) |
| R7 | نقطة ربط اختبار runScanPipeline/showOcrResults/searchCandidates على النافذة | إنتاج غير متأثر؛ الاختبار يقود المسار الحقيقي بلا موك | يثبت الأتمتة المستعادة تنفيذيًا بما فيها recognize() كامل بمحرك OCR حقيقي |
| R8 | SW v23→v24 والنسخة 1.6.0→1.7.0 في نفس الجولة | كل مرحلة واجهة ترفع الكاش (قاعدة دائمة) | أُروَّج مرة واحدة عند الإقفال لتقليل تحديثات المستخدم |

## 2026-09-25 — قرارات المرحلة ج (فصل وضعي المزارع/المحترف)

| # | البند | القرار/الافتراض | السبب |
|---|---|---|---|
| C5 | الفلترة عرضية في render لا في المحرك | shownResults تفصل البطاقات المعروضة فقط؛ SearchCore يبني النتيجة نفسها في الوضعين | قاعدة البرومبت: «لا تعديل في محرك المطابقة والحكم إلا ما يُذكر صراحة»؛ الفرق بين الوضعين عرض لا حكم |
| C6 | بطاقات ليبيا (248 و500) تُعرض في الوضعين | المرجع القانوني الفعلي للمستخدم الليبي لا يُحجب بتبسيط الواجهة | بند «الحالة الليبية في الوضعين»؛ حظر 248 قرار سلامة لا خيار عرض |
| C7 | شريط الحظر القطعي يُبنى من مصفوفة 248 بعد الفلترة وبشرط classify=exact | لا شريط قطعي بلا تطابق تام حقيقي من قاعدة 248 المطابقة فعلًا | منع أي شريط حظر مُختلَق من نتائج دول أخرى؛ الحظر الاحتمالي يبقى في بطاقته |
| C8 | شريط الإخلاء انتقل من سطر ثابت في index.html إلى ترويسة مُصيَّرة مع كل دفعة نتائج | نفس المفتاح data-i18n="disclaimer.strip" ونفس النص حرفيًا؛ الحذف من HTML يمنع التكرار المزدوج | يجعل الإخلاء حاضرًا في المسارين (بحث/مسح) وفي كل إعادة تصيير لغة/وضع بدل غيابه عن عروض لا تليه |
| C9 | SW v22 + الإصدار 1.6.0 في نفس الإيداع | سلوك واجهة جديد معروض | قاعدة رفع الكاش الدائمة |
| C10 | اختبار mode-split يستعلم DDT على القواعد الحقيقية | DDT: تطابق تام 100% في 248 وموجود في قواعد أخرى → يُظهر الفرق بين الوضعين على نفس الصف | يثبت الجدول مواصفةً لا بالموك؛ تشغيل بمتصفح حقيقي 14/14 |

## 2026-09-25 — قرارات المرحلة ب (المعالجة الحية) والمرحلة ج (حماية القراءة)

| # | البند | القرار/الافتراض | السبب |
|---|---|---|---|
| B10 | زر «تصوير الملصق» يبدأ البث الحي مباشرة | المسار الثابت (التقاط نظامي) يبقى متاحًا على أضعف الأجهزة حيث `PROFILE.weak.live = false` عبر الزر نفسه | نص البرومبت يطلب استبدال «التقاط → انتظار → نتيجة» بالمعالجة الحية؛ الكاميرا الحية هي المسار الأساسي الآن، والإسقاط النظامي مسار احتياطي للأجهزة التي لا يُشغَّل عليها الحي أصلاً |
| B11 | مهلة تبريد 8 ثوانٍ للتمريرات التلقائية | إطار واحد للمحرك الكامل على الأكثر كل 8 ثوانٍ أثناء الحلقة الحية | بلا قياس ميداني بعد؛ قيمة محافظة تمنع تكرار المحرك الثقيل على كل إطار ناجح (البرومبت: «بلا تجميد أبدًا») — تُعدَّل بالقياس الميداني |
| B12 | «التقاط أفضل إطار» موحّد على كل الأجهزة | الحلقة الحية تتوقف أثناء الالتقاط اليدوي ويعود إيقاعها بعده؛ الالتقاط دائمًا أفضل-من-3 | بسيط وموحد: مساران فقط (حية تلقائية / التقاط يدوي) بدل ثلاثة سلوكيات متغايرة |
| B13 | عودة الحلقة بعد التقاط يدوي ناجح | النجاح يوقف الكاميرا كليًا (`stopLive(false)`) — حالة النتيجة هي الجديدة | ب1 «النجاح المبكر يوقف المعالجة الحية»؛ لا معنى لبث يعمل تحت نتيجة معروضة |
| B14 | نسبة الإطار الإرشادي 72%×34% | حجم أولي لعنصر المادة الفعالة داخل الإطار، قابل للتعديل بعد القياس الميداني | لا رقم في البرومبت؛ افتراض موثق |
| C3 | الحماية مفاتيحها `ocrBusy` وحدها | لا مغادرة أثناء القراءة فقط؛ بلا علاقة بالكاميرا الحية أو النتائج المعروضة | بروتوكول المراجعة الميدانية (docs/FIELD_REVIEW.md §ج): «نتيجة مسح لا يجب أن تظهر لمستخدم ليس على شاشة المسح» — حماية قراءة لا سجن تنقل؛ التصميم لم يُمس |
| C4 | بلا إيداع للمرحلة ج قبل اكتمالها مع اختبارها | انعكس التقسيم المخطط (ب ثم ج) لسبب عملي موثق: حارس ج سطور داخل applyView في app.js نفسه الذي يحمل كود ب، وstage-b.test.mjs فحوصاته تمزج ب وج في ملف واحد — إيداع ب وحده كان سيُدفع فاشل اختباراته | سابقة A9 نفسها (إيداع واحد لمرحلتين لأن الملفات مشتركة بعمق)؛ التسليم الفعلي: إيداع كود (ب+ج مكتملتان باختباراتهما الخضراء) ثم إيداع وثائق |
| 15 | SW v19 + إصدار 1.5.0 في نفس الإيداع | ملف جديد في الهيكل (scan-live.js) وسلوك واجهة جديد | قاعدة رفع الكاش الدائمة |

## 2026-09-25 — قرارات المرحلة أ (الأتمتة الكاملة ومسح النتائج القديمة)

## 2026-09-23 — قاعدة أمريكا الماستر (ب) + حد الثقة (أ4)

| # | البند | القرار/الافتراض | الدليل |
|---|---|---|---|
| B1 | نسخة EPA 2,199 (تجميع غير موثق) استُبدلت ببناء الدفتر | إعادة بناء كاملة بأمر المستخدم (المرحلة ب)؛ أداة البناء تؤكد أرقام التحقق الداخلية (5,091/1,216/1,331/319/5,486) قبل الكتابة | `tools/build-epa-master.js`؛ docs/data-provenance.md §4–5 |
| B2 | 2,544 مادة بلا صفوف منتجات استُبعدت من الملفين | إدراجها «محظورة» يخترع حالة قانونية لم يصرّح بها EPA | تعليق الأداة + data-provenance §5 |
| B3 | وسما «مقيد» (RUP) سقطا من EPA الجديد | عمود RUP غير موجود في الدفتر الجديد — لا يُستعاد بالتخمين؛ إعادته تحتاج مصدر PPIS نفسه | docs/data-provenance.md §4 |
| B4 | حقل `row` أُضيف للملفين المبنيين | كل قواعد أخرى تشير للصفوف به؛ غيابه طبع `#undefined` في تقرير الأزواج المتقاربة | إصلاح موثق في الأداة + اختبار data-health |
| B5 | خطوط أساس الانحدار أعيد حسابها (مكررات 46→282، CAS 11→12، أزواج متقاربة 1,559→2,267) | نتيجة حتمية لتغير القاعدة، موثقة بأسمائها الحرفية | tests/data-health.test.mjs + docs/near-pairs.md |
| B6 | بروب زوج الستيريو (E)/(Z) استُبدل ببروب Esfenvalerate المكرر داخل EPA | زوج الستيريو غير موجود في البيانات المبنية؛ Esfenvalerate فخ اسم مكرر حقيقي (CAS مختلف) يحمل نفس العقد الأمني | tests/namepairs.test.mjs |
| A4a | حد الثقة = 45 | تجريبي: أعلى خردة 24، أدنى ملصق صحيح 90؛ تفصيل الاختيار في وثيقة المقارنة | docs/ocr-engine-comparison.md قسم أ4 |
| A4b | الأدلة المهيكلة تُعفى من بوابتَي النسبة والثقة — ثم **مُضيَّق بقرار المستخدم**: الإعفاء حصرًا لرقم CAS اجتاز فحص رقم التحقق (`casChecksum === true`)؛ منطقة المادة الفعالة وأرقام CAS الفاشلة لا تُعفي (هي مع ذلك غير كافية وحدها لقبول مؤكد: القواعد تشترط رقمًا صالحًا أو اسمًا مؤكدًا). حالات cas_* الأربع في E2E تستخرج رقمًا صالحًا فتبقى حية، وepa_reg_negative (رقمه فاشل) تبقى مرفوضة | src/ocr.js (hasValidCas)؛ فحص حي 7/7؛ E2E 20/20 |
| A4c | رسالة رفض الثقة مميزة عن رسالة تسرب العربية | تشخيص ميداني أدق؛ النص الحرفي في القواميس الأربعة | `ocr.rejected.conf` في src/i18n.js |
| A7 | أداة المقارنة أداة تطوير فقط ولا تُشحن | تجلب محركًا تجريبيًا من esm.sh وقت التجربة فقط؛ التطبيق المنشور بلا أي اتصال خارجي (CSP كما هي) | tests/ocr-compare.mjs رأس الملف |
| A8 | لا تبديل محرك في هذه الجولة — التوصية مكتوبة بانتظار قرار المستخدم | بند أ3 صريح في البرومبت؛ القياس الحي: 0 قبول خاطئ/0 رفض خاطئ للمحرك الحالي | docs/ocr-engine-comparison.md «التوصية» |
| A9 | إيداع واحد للمرحلتين بدل اثنين | الملفات مشتركة بعمق (i18n/app/sw/verify)؛ إيداع وسطي كان سيُدفع اختباراته حمراء — نقيض قاعدة «لا إيداع قبل اكتماله» | هذا السجل |
| C1 | المرحلة ج (زر المشاركة) نُفِّذت في جولة المتابعة بعد سؤال المستخدم الصريح | الزر كان صامتًا بلا Web Share؛ الآن ثلاثة مسارات مع إشعار بالنتيجة: Web Share → حافظة (isSecureContext) → بطاقة vCard بالرابط | src/app.js (#shareBtn)؛ i18n ×4 |
| C2 | رفع الكاش v17 مع المرحلة ج والتضييق | كلاهما يغيّر سلوكًا معروضًا للواجهة | sw.js |
| A10 | PP-OCRv5 (paddleocr-js 0.4.2) غير مقيس | تنزيل النموذج من esm.sh تعذر في جلسة بلا تنزيلات حزم؛ لا تُخترع أرقام | docs/ocr-engine-comparison.md |

## 2026-09-21 — جولة الواجهة (مطابقة تصميم المرجع)

| # | البند | القرار/الافتراض | الدليل |
|---|---|---|---|
| W1 | اللقطات المرفقة | **لم تصل مع البرومبت** — التنفيذ من مواصفات القسم 3/4/5 النصية + الرابط المرجعي (يُرسم بالجافاسكربت، فشل الجلب النصي) | هذا السجل |
| W2 | الخط | IBM Plex Sans Arabic v15، وزنان 400/700 (~39KB)، OFL 1.1 — النص الكامل مرفق في المستودع | assets/fonts/LICENSE-OFL-IBM-Plex-Sans-Arabic.txt |
| W3 | الأيقونات | Lucide v0.462.0 (ISC)، 31 رمزًا، inner markup حرفي؛ `alert-triangle` أُعيدت تسميته `triangle-alert` كما في المصدر | assets/icons/LICENSE-LUCIDE-ISC.txt |
| W4 | الرأس | الوضع (مزارع/محترف) قرص select في الرأس كالمرجع؛ رقاقات الحالة أُلغيت ككيان وانتقلت أعدادها للرئيسية والقواعد | index.html |
| W5 | السمة | الداكنة هي الافتراضية (كالمرجع)؛ الفاتحة مشتقة بنفس البنية وتجتاز تباين 4.5 (مقاس) | tests/ui-browser.test.mjs |
| W6 | 500 كهرماني | الاستثناء المطلوب مطبق: لا أخضر «آمن» في أي حالة؛ الأحمر لعدّاد المحظورات وحده | src/app.js + لقطة home |
| W7 | نص «results.ambiguous» | السهم ↔ استُبدل بكلمة لغوية (مقابل/vs/contre/与) لأنه رمز وليس إيموجي؛ باقي النصوص حرفيًا كما كانت | src/i18n.js |
| W8 | بطاقة المطور | صورة `assets/developer.jpg` تُقرأ من مسارها؛ غابت فدائرة بأيقونة (لا اختراع صورة) | index.html + app.js |
| W9 | Chrome 109 | فحص ثابت في verify.mjs (لا backdrop-filter/حركة مستمرة/ميزات >109)؛ تشغيل 109 نفسه **لم يُقَس** — اختبار يدوي على جهاز المستخدم | tests/verify.mjs |
| W10 | تحديث محددات الاختبارات | verify.mjs: بندا «statusline chips» استُبدلا ببنود العروض الستة والبطاقات؛ بند SW v7→v8؛ إشعار OCR من index.html إلى i18n.js. لا تغيير في معرفات العناصر التي تعتمدها E2E | tests/verify.mjs |
| W11 | زر مسح الكتابة في البحث | طلب صريح بعد التسليم: أيقونة معنى جديد `clear-query` → `circle-x` (من Lucide v0.462.0 نفسها، لا كسر لقاعدة رمز↔معنى). زر 44px داخل الحقل يظهر عند وجود نص فقط، يمسح ويعيد التركيز للكتابة أو اللصق. تزامن مع الملء من السجل. العنوان عبر القواميس الأربع. الكاش → v9، الإصدار → 1.3.1 | index.html، src/app.js، src/icons.js، src/i18n.js، sw.js، version.json، tests/verify.mjs |

## 2026-09-21 — إقفال جولة «الإصدار الثاني»

| # | البند | القرار/الإجراء | الدليل |
|---|---|---|---|
| 11 | دفع إيداعات الجولة (7 إيداعات) إلى main | نُفِّذ بعد نجاح كل الاختبارات: `3a05852..4267d00` | `git ls-remote` = `4267d00d7b8d8238a09fd35022a3bd21b70140a8` = محلي main |
| 12 | الاختبارات النهائية قبل الدفع | ثابت 557 ناجحًا / 0 فاشل؛ E2E V2 20/20 بمتصفح حقيقي (تسرارات حقيقية، Tesseract حقيقي، القواعد الأربع) | `/tmp/e2e/v2-results.json` (خارج المستودع) |
| 13 | رموز 500 وتواريخ القرارين وشرح REV* | ما زالت بانتظار نص المستخدم — لم تصل في هذه الجلسة؛ لا تفسير من الوكيل، وتُعرض الرموز حرفيًا | docs/data-provenance.md |
| 14 | الصور السبع عشرة وورقة الحقيقة المرجعية | لم تُرفع بعد؛ د1 (الخط الأساسي) يبقى متوقفًا عند الجاهزية بلا قياس | docs/ocr-baseline-v2.md |
| 15 | ز4 (تفعيل Pages) | ما زالت بيد المستخدم خطوة يدوية واحدة من إعدادات المستودع | docs/PROGRESS.md |

## 2026-09-20 — أثناء المرحلة ز (النشر)

| # | البند | القرار/الافتراض | الدليل |
|---|---|---|---|
| 7 | انحدار E2E (0/20) اكتُشف أثناء ز | إصلاح فوري قبل أي نشر: حارس إعادة دخول في `status()` (`66f7087`) | E2E 20/20 بعد الإصلاح |
| 8 | تحديث حارس المسارات الثابت في `verify.mjs` بعد ز1 | تغيير اختبار معلَن ومقصود (البنية الجديدة `appRoot()`)؛ فحص منع CDN بقي كما هو | `cfc7f24` |
| 9 | Pages API محجوب لصلاحيات التكامل (403) | خطوة يدوية واحدة للمستخدم: Pages → Source → **Deploy from branch → main → /(root)** | docs/PROGRESS.md |
| 10 | 404 لـ `data/fao.json` أثناء التشغيل | مقصود: وحدة FAO معطّلة fail-soft بقرار ج4؛ ليس عيبًا | تحقق بالتشغيل (probe 404) |

| # | البند | القرار | السبب |
|---|---|---|---|
| 1 | حذف لوحة تأكيد المواد (أ3) | حُذفت الحالة الإلزامية كليًا: النص الناجح يُعرض تلقائيًا | نص البرومبت يطلب صراحةً «بلا أي زر أو خطوة يدوية بينهما»؛ التصميم المتعدد المواد محفوظ — كل النتائج تُعرض معًا والرقاقات كخيار إعادة بحث يدوي عبر محرر النص «بحث من النص» كما هي |
| 2 | منع النتائج المعلقة (أ1) | عدّاد توليد `scanSeq/activeScanSeq` + حرمان كل تحديثات الواجهة بعد recognize وفي catch | إلغاء `OcrModule.cancelCurrent()` تعاوني فقط (حدود التمرير) ولا يوقف العمل الجاري فورًا — الحارس الوحيد المضمون أن نتيجة مسح أُحل محله لا ترسم أبدًا |
| 3 | عارض النتائج في شاشة المسح | نفس دالة `render()` مع وسيط هدف `#scanResults` (المسار اليدوي افتراضي `#results`) | بلا منطق عرض ثانٍ؛ نفس طبقة القرار والحالة والحكم؛ إعادة العرض عند تغيّر اللغة/الوضع تشمل المسارين |
| 4 | مفاتيح i18n الجديدة | `scan.cancelImage` + `ocr.auto` في القواميس الأربعة (ar/en/fr/zh) | حارس no-hardcoded-arabic يمنع العربية خارج t()/tf()؛ الاتساق مع اللغات الموجودة |

## 2026-09-20 — بوابة أسئلة جولة «من التدقيق إلى النشر»

| # | البند | قرار المستخدم | الأثر على التنفيذ |
|---|---|---|---|
| 1 | الصور والحقيقة المرجعية | **ستُرفع لاحقًا** | المرحلة د1 (الخط الأساسي الحقيقي) تتوقف عند الجاهزية: مجلد `tests/fixtures/labels/` وقالب `docs/ocr-baseline-v2.md` يُجهّزان فقط، ولا قياس حتى وصول الصور |
| 2 | دلالة رموز القرار 500 (`Approved/RAR/REV/REV*`) | المستخدم طلب **شرحًا كتابيًا الآن** | يجري البحث عن نص القرار الرسمي وتوثيق ما يثبت بمصدره؛ كل رمز لا يثبت مصدره يُعرض كما هو + «رمز غير مفسَّر». لا تفسير قانوني من الوكيل. **بانتظار تأكيد المستخدم للشرح** |
| 3 | مفردات الحالات | تُطبَّق الصياغة المحايدة المقترحة **وسيعدّل المستخدم بعض الصياغات** | تُبنى المفردات في قاموس i18n مفاتيح مستقلة ليسهل تعديل نصوصها دون كود |
| 4 | الترخيص | **بلا ملف ترخيص الآن** | لا يُنشأ LICENSE. حقوق الشيفرة تبقى للمطوّر. ملف ترخيص الطرف الثالث (توريسيركت) يُعاد حفظه لأن ترويسة الملف تشير إليه |
| 5 | الهوية | افتراض محافظ | زر «ادعم أو تواصل» يقرأ `config/support.json` ويُخفى إذا كان فارغًا (هو فارغ الآن) |
| 6 | الفرعان البعيدان | **أرشفة الآن + موافقة على دمج `79b2829` لاحقًا** | يُؤرشف الفرعان بوسم وفرع على GitHub الآن؛ الدمج لا يُنفَّذ في هذه الجولة — قرار مستقبلي بالكامل بيد المستخدم |

## جولة 2026-09-25 — قرارات المرحلة أ (بطء القراءة الثابتة)

| # | البند | القرار/الوقيعة | الأثر |
|---|---|---|---|
| أ-1 | شكل إصلاح البوابتين | قفل مبكر (earlyLock) عند exactHit/CAS صالح داخل الحلقة بدل return acceptResult الحرفي من البرومبت — بنية recognize() ذات نقطة إرجاع واحدة ودمج تراكمي يجعلان الخروج من منتصف الحلقة يفقد دور ROI/الدمج | جوهر المنطق محفوظ (توقف فوري + عدم هدر المؤكد)؛ موثق في docs/ocr-speed-diagnosis.md |
| أ-2 | رفض قفل «اجتياز البوابتين» | المحاكاة على الأدلة أثبتت أنه يقفل rotated_90 عند محاولة conf=49 بلا تطابق قاعدة — قبول غير مؤكد (انحدار بالخط الأحمر) | القفل على التأكيد القوي حصرًا (exactHit / CAS صالح) |
| أ-3 | صور الاستنفاد بلا تأكيد (7.jpg 111ث، level3_damaged 104ث) | لا علاج في هذه الجولة — لا تأكيد يُهدر فيها؛ المشكلة حدود المحرك/المعالجة المسبقة (فرع 3 من الشجرة) | أولوية تالية موثقة؛ بلا تغيير إعدادات المحرك |
| أ-4 | src/ocr-diagnostics.js في الإنتاج | خاملة تمامًا بلا تفعيل صريح (sink فارغ + حارس lazy)؛ خارج SHELL عمدًا | صفر كلفة على المستخدم؛ تتوجه بأداة التطوير فقط |

## جولة 2026-09-25 — قرار المرحلة ب (مسح السجل)

| # | البند | القرار/الوقيعة | الأثر |
|---|---|---|---|
| ب-1 | الخلل المرصود ميدانيًا | لم يَعُد يُعاد إنتاجه على الكود الحالي: idbClear يلمس المخزن الدائم فعلًا (إثبات: getAllKeys=[] بعد النقر وعدم العودة بعد reload كاملة) | قُدِّم تشخيص حقيقي بدل افتراض خلل؛ الإحكام: promise من openHistory + إشعار النتيجة في كل المسارات (cleared/clearFail ×4 قواميس) |

## ثوابت متراكمة من الجولات السابقة (لا تُنقض إلا بقرار صريح)

- قواعد البيانات الأربع (ليبيا 248/500، EU، EPA) لا تُمس محتواها؛ **ليبيا هي المرجع الأول**.
- حد المطابقة 80% في SearchCore.
- OCR محلي ذاتي الاستضافة (tesseract.js 6.0.1، eng-only بعد إزالة ara)، لا OCR سحابي، لا CDN.
- التطبيق Offline-first؛ لا اتصال بأي نطاق خارجي أثناء التشغيل؛ لا تُوسَّع CSP.
- عدم العثور على مادة ≠ مسموحة؛ لا اختراع حالة قانونية؛ قيم المصادر القانونية تُعرض حرفية مع ترجمتها بجانبها.
- لا دفع قسري، لا إعادة كتابة تاريخ، لا حذف فروع/وسوم بعيدة.
