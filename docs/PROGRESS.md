# سير العمل والتقدم — المستشار الزراعي

> يُحدَّث عند كل بوابة. الأعمدة: المرحلة، الحالة، الإيداع، الدليل.

| المرحلة | الحالة | الإيداع | الدليل |
|---|---|---|---|
| بوابة الأسئلة (6 بنود) | ✅ أُجيبت 2026-09-20 | — | docs/DECISIONS.md |
| أ) الحماية والتوحيد | ✅ مكتمل | b754f4c | حزمة backup-2026-09-20.bundle + وسوم أرشيف ×3 مدفوعة + نقل الشيفرة الميتة archive/ + README |
| ب) سلامة القرار والمطابقة | ✅ مكتمل | cf2ff79 | src/cas.js + namepairs 19/19 + nearpairs 400/400 + 13 خطأ CAS موثّق لا يُصحّح |
| ج) البيانات والمصادر | ✅ مكتمل | cf2ff79 | docs/data-provenance.md + data-health 18/18 + FAO معطّل بعلم موثّق |
| د) التعرف البصري | 🔄 د2–د5 منجزة · د1 (الخط الأساسي) متوقف على الصور · د3 (محرك بديل) لاحقًا | — | probeImage + تأكيد + إلغاء + تشخيص محلي + واجهة scan() الموحدة |
| هـ) تعدد اللغات | ✅ مكتمل | 051e9bc | رسائل OCR انتقلت للقواميس + اختبار النص العربي الثابت 2/2 + dir/lang حيوي |
| و) التراخيص والخصوصية والدعم | ✅ مكتمل | (إيداع و) | tesseract.min.js.LICENSE.txt مستعاد + THIRD_PARTY.md + about/privacy + زر دعم من config/support.json |
| ز) النشر للمراجعة الميدانية | ✅ مكتمل | (إيداع ز) | SW v7 + precache الجديد + تحقق E2E بعد إصلاح انحدار status() |
| انحدار E2E (اكتُشف أثناء ز) | ✅ مُصلَّح | 66f7087 | انهيار المكدس في status()↔ensureWorker — حارس إعادة دخول في src/ocr.js؛ E2E 20/20 |
| ز1) توافق المسار الفرعي | ✅ مكتمل | cfc7f24 | worker/core/lang عبر appRoot() بدل الجذر الأصلي؛ static 99/99؛ E2E 20/20 |
| ز5) التحقق دون اتصال (محلي) | ✅ بالتشغيل | — | SW نشط؛ القواعد 77/411/1483/2199؛ بحث Glyphosate 100% بلا شبكة؛ 404 الوحيد = fao.json (معطّل عمدًا) |
| ز4) نشر GitHub Pages | ⏳ خطوة يدوية واحدة | — | واجهة API للنشر محجوبة (403): Pages → Source → main /(root) من إعدادات المستودع |
| إقفال الجولة: اختبارات نهائية + دفع main | ✅ مكتمل | 4267d00 | ثابت 557/557 (verify 99 + data-health 18 + judge 38 + nearpairs 400 + i18n 2) + E2E V2 20/20؛ دُفع 3a05852..4267d00 إلى origin/main |
| واجهة: مطابقة تصميم المرجع | ✅ مكتمل | 74beb48 + bb6dbbf | 6 عروض hash + سمتان + خط Plex OFL + أيقونات Lucide ISC؛ ثابت 110+6+7، E2E 20/20، لقطات 13؛ دُفع وتحقق من الرابط المنشور: version.json 1.3.0، woff2/icons/icons.js ‏200، sw v8 مسجّل |
| مؤجَّل: docs/AGENTS.md | ✅ مكتمل | (إيداع و) | وثيقة صلاحيات الوكلاء 0/1/2 بلا أي وكيل خلفي |

## جولة 2026-09-23 — قاعدة أمريكا الماستر (المرحلة ب) + حد الثقة (أ4)

| البند | الحالة | الدليل |
|---|---|---|
| ب) إعادة بناء EPA من الدفتر الرسمي | ✅ مكتمل | `tools/build-epa-master.js` يقرأ `sources/EPA_Master/EPA_Master.xlsx` ويؤكد أرقام التحقق الخمسة قبل الكتابة؛ `epa.json` = 1,361 (النشط) و`epa-cancelled.json` = 1,425 (الملغى) بحقل `row` تسلسلي و`status_raw` عربي حرفي؛ data-health 23/23؛ واجهة (بطاقة/قاعدة/رتبة/تحكيم/قواميس ×4) مكتملة؛ SW v15/v16 يجهّزان القاعدة الخامسة |
| أ4) حد الثقة للقراءة غير المهيكلة | ✅ مكتمل | `MIN_CONFIDENCE = 45` في مسار OCR الحي؛ بروب حي: الضجيج (ثقة 24) يُرفض `ocr.rejected.conf` والملصق (90) يُقبل؛ اختبار ثابت 21/21؛ SW v16 |
| أ2/أ3) أداة المقارنة + التوصية | ✅ موثقة | `tests/ocr-compare.mjs` (أداة تطوير فقط)؛ قياس حي للعينة الكاملة: 0 قبول خاطئ، 0 رفض خاطئ؛ لا مفاتيح وصول؛ PP-OCRv5 غير مقيس (لا تنزيلات وقت الجلسة)؛ التوصية: **لا تبديل محرك** — بانتظار قرار المستخدم في `docs/ocr-engine-comparison.md` |
| إصلاح انحدار E2E المكتشف | ✅ مُصلَّح | حالات cas_* الأربع (صورة CAS فقط ≈58% لاتيني) كانت تُرفض بالبوابة اللاتينية من الجولة السابقة → الأدلة المهيكلة (CAS/منطقة المادة) تُعفى من البوابتين؛ E2E 20/20 بعد الإصلاح |
| الاختبارات النهائية | ✅ كلها خضراء | ثابت 539 فحصًا (9 ملفات) + verify 115/115 + متصفح 11/11 + E2E 20/20 |

## ملاحظات التشغيل (حدود المنصة المطبقة)

- الأوامر تُقتل بعد ~30–180 ث؛ الذاكرة 2 غيغابايت؛ لا عمليات خلفية طويلة → كل خطوة طويلة تُقسّم.
- الاختبار الثابت: `node tests/verify.mjs` — الوظيفي: جناح الـ20 حالة عبر متصفح حقيقي في `/tmp/e2e` (خارج المستودع).

### تعديل صورة المطور — 2026-09-21
- طلب المستخدم: العرض الدائري الأنيق. نصف القطر 50% + حلقة تحديد رقيقة عبر ظل مزدوج. بلا أي تغيير آخر (الكاش v10، الإصدار 1.3.2). **ملف `assets/developer.jpg` لم يصل مساحة العمل بعد** — عند إضافته يظهر تلقائيًا بشكل دائري (الكود يقرأ المسار ويخفي البديل عند نجاح التحميل).

### تسليم جولة الواجهة — 2026-09-21
- إيداعان تقدميان إلى main: `74beb48` (التنسيق + الأصول + الاختبارات) ثم `bb6dbbf` (سكربتا اللقطات والفحص).
- تحقق من الرابط المنشور (بالتشغيل): version.json = 1.3.0/released 2026-09-21، كل الأصول الجديدة 200، index.html المنشور يطابق المحلي (data-view + الخط + التنقل)، sw.js = mustashar-v8 وserviceWorker.register موجود.
- لم يُقَس: كروم 109 فعليًا (فحص ثابت فقط) — اختبار يدوي على جهاز المستخدم.
- الملاحظات النصية المؤجلة مسجلة في DECISIONS (قسم جولة الواجهة) وتُعالج في جولة مستقلة.
