# سير العمل والتقدم — المستشار الزراعي

> يُحدَّث عند كل بوابة. الأعمدة: المرحلة، الحالة، الإيداع، الدليل.

## جولة 2026-09-26 — إصلاح انتكاسات حرجة بعد جولة التشخيص والوضعين (أ/ب/ج/د)

| البند | الحالة | الدليل |
|---|---|---|
| المرحلة صفر) تحديد نطاق الضرر من تاريخ جيت | ✅ مكتمل | الفرق 37401c2..e30fc48 قرأ كاملًا: انتكاسة أ = فلتر shownResults في render (f1058dd) طُبّق على عرض المسح؛ انتكاسة ب = التصنيف خلف showDetails منذ cf2ff79 (قِدَم تعرّض للانكشاف بالفصل)؛ ج موجودة شيفرةً (diagExport JSON خام)؛ د = إشعارات المشاركة في #ocrMsg المخفية خارج شاشة المسح. ocr.js في جولة التشخيص أضاف القفل المبكر فقط بلا مسّ للبوابات |
| أ) استعادة الأتمتة الكاملة | ✅ مكتمل | اختبار جديد tests/scan-automation.test.mjs بمتصفح حقيقي 9/9: نتائج المسح تظهر في الوضعين (EPA الظاهرة في المزارع)، البحث اليدوي يبقى مفلترًا، وrecognize() كامل بمحرك OCR حقيقي على صورة Warfarin المولّدة أنتج النتائج تلقائيًا بلا أي نقرة. إصلاح جانبي: تبديل الوضع يعيد التصيير وفق الحالة المحفوظة |
| ب) التصنيف الوظيفي في الوضعين | ✅ مكتمل | خرج من شرط showDetails؛ mode-split الآن 16/16 يثبت الرقاقات في المزارع والمحترف معًا (DDT = I حشري) مع بقاء raw/matchType للمحترف حصرًا |
| ج) تصدير التشخيص المقروء | ✅ مكتمل | HTML عربي RTL بعناوين واضحة عبر i18n ×4 (لا مفاتيح JSON خام)؛ الاختبار ينقر الزر ويلتقط التنزيل الحقيقي عبر CDP ويقرأ الملف من القرص: 15 تأكيدًا |
| د) زر المشاركة بتتبع تنفيذ فعلي | ✅ مكتمل | trace 1: Web Share استُدعي فعليًا بعنوان التطبيق؛ trace 2: writeText استُدعي + إشعار «نُسخ» مرئي في الشاشة نفسها؛ trace 3: فشل الحافظة (NotAllowedError) سقط لبطاقة vCard حقيقية نُزّلت + إشعار يذكر الاستثناء بالاسم؛ 15/15 ×تشغيلين متتاليين. الاكتشاف الجذري: الإشعارات كانت في عنصر مخفي + إشعار الفشل العابر كان يُستبدل بإشعار vCard |
| لا تعديل على منطق القراءة/المطابقة | ✅ مُثبَت | src/ocr.js لم يُمَس في هذه الجولة؛ verify يشمل فحص القفل المبكر والبوابات كما هي؛ بوابات 11/21/26 + early-confirm 12 خضراء |
| الاختبارات الكاملة دون انحدار | ✅ كلها خضراء | ثابت: verify 119 + stage-a 24 + stage-b 36 + namepairs 17 + nearpairs 400 + judge 38 + data-health 23 + status-legend 39 + ocr-eng 11 + ocr-latin 21 + ocr-conf 26 + early-confirm 12 + eng-only-arabic 2 + icons 6 + no-hardcoded-arabic 2؛ متصفح: ui 11/11 + history-clear 6/6 + mode-split 16/16 + scan-automation 9/9 + share-diag 15/15؛ E2E العشرون 20/20 |
| رفع الكاش والإصدار | ✅ مكتمل | SW v22→v24 (ترويسة v23 للاستعادة، v24 للتصنيف المشترك)؛ الإصدار 1.6.0→1.7.0 (version.json + package.json + ترويسة stage-b) |
| حدود الجولة (صادق) | 📌 | لم يُقَس على كروم 109 فعليًا؛ مؤجل عمدًا حسب البرومبت: خلط شرح رموز ليبيا بحالة EU، ظهور نتائج 85% بجانب 100%، أي تعديل في جودة/سرعة القراءة (ينتظر صور المستخدم)؛ تتبع المشاركة الحي صُنع في كروم مكتبي حديث — سلوك Web Share على iOS/أندرويد حقيقي يبقى لاختبار يدوي على الهاتف |

## جولة 2026-09-25 — المرحلة ج: فصل وضعي المزارع/المحترف (بند «الوضعين» من برومبت البطء)

| البند | الحالة | الدليل |
|---|---|---|
| ج1) بطاقات الدول الأخرى في المحترف فقط | ✅ مكتمل | فلترة عرضية في render() بـ shownResults: المزارع يرى بطاقات ليبيا 248/500 حصرًا؛ المحترف يرى كل القواعد (ليبيا + EU/EPA/الملغى) — SearchCore والمحرك لم يُمَسا |
| ج2) رقم CAS في المحترف فقط | ✅ مكتمل | فقرة CAS (p.meta) كانت معبوّة بـ showDetails من قبل؛ الثبوت الآن حي: المزارع 0 فقرة CAS، المحترف فقرات (mode-split) |
| ج3) شرح الرمز (status_raw/التصنيف/نوع المطابقة) في المحترف فقط | ✅ مكتمل | raw/cat/matchType كلها خلف showDetails؛ الحالة الليبية ورقاقة التصنيف الوظيفي تبقيان في الوضعين؛ رقاقة «شرح الرموز» (st-explain) محترف فقط |
| ج4) شريط الإخلاء يظهر في الوضعين | ✅ مكتمل | ترويسة disclaimer-strip تُصيَّر الآن مع كل دفعة نتائج داخل render() (بحث ومسح) بنص data-i18n نفسه؛ السطر الثابت في index.html حُذف كيلا يتكرر؛ لا يغيب عن أي عرض |
| ج5) شريط الحظر القطعي (تطابق تام 100% في قرار 248) يظهر في الوضعين ولا يُبسَّط | ✅ مكتمل | results.ban.absolute ×4 قواميس + CSS .prohibited-absolute + data-ban-banner="exact"؛ مبني من مصفوفة 248 بعد الفلترة فلا يظهر إلا لمطابقة حقيقية 100% |
| قواميس ×4 | ✅ مكتمل | results.ban.absolute في ar/en/fr/zh — no-hardcoded-arabic PASS |
| الاختبارات | ✅ كلها خضراء | اختبار جديد tests/mode-split.test.mjs بمتصفح حقيقي على القواعد الحقيقية (DDT): 14/14 يثبت جدول الفروق حرفيًا (المزارع 0 فقرات تفصيل، المحترف فقرات؛ شريطا الإخلاء والحظر في الوضعين؛ بطاقة ليبيا لا تُحذف من المزارع)؛ ثابت: verify 119 + stage-a 24 + stage-b 36 + namepairs 17 + nearpairs 400 + judge 38 + data-health 23 + status-legend 39 + ocr-eng/latin/conf 11/21/26 + early-confirm 12 + eng-only-arabic 2 + icons 6؛ متصفح: ui-browser 11/11 + history-clear 6/6؛ E2E العشرون 20/20 بعد التعديل |
| رفع الكاش والإصدار | ✅ مكتمل | SW v21→v22 بترويسة مطابقة؛ الإصدار 1.5.0→1.6.0 في version.json + package.json؛ ترويسة verify.mjs حُدِّثت |
| حدود الجولة (صادق) | 📌 | لم يُقَس على كروم 109 فعليًا (فحص ثابت فقط كما في كل الجولات)؛ بطاقات ليبيا-500 الملغى في المزارع بنفس تفصيل بطاقة 248 (الحد الأدنى المطلوب)، وعمّقها إن طُلب؛ التحقق الحي من الرابط المنشور بعد الدفع أدناه |

## جولة 2026-09-25 — المرحلة ب: المعالجة الحية المستمرة + المرحلة ج: حماية القراءة الجارية

| البند | الحالة | الدليل |
|---|---|---|
| ب1) معالجة حية مستمرة من تدفق الكاميرا | ✅ مكتمل | زر «تصوير الملصق» يبدأ بثًا حيًا (بلا صورة ثابتة); إطارات مصغّرة تُفحص رخيصًا (حدة + كثافة حواف) كل 500ms؛ أول إطار ناجح يُحلَّل بالمحرك الكامل مع مهلة تبريد 8s للتمريرات التلقائية؛ النجاح المبكر يوقف الحلقة ويعرض النتيجة |
| ب2) التقاط أفضل إطار | ✅ مكتمل | زر «التقاط أفضل إطار» يأخذ 3 إطارات متتالية ويختار الأوضح (تباين محلي) — على كل الأجهزة، موحّدًا |
| ب3) إطار إرشادي + قصّ ROI | ✅ مكتمل | إطار شبه شفاف 72%×34% في منتصف الفيديو؛ المحرك الكامل يقرأ المنطقة داخل الإطار مكبّرة فقط؛ الإطار الكامل غير المقصوص يُعرض في المعاينة/السجل (canvas→blob) |
| ب4) تفاوت الأجهزة | ✅ مكتمل | أضعف الأجهزة (نوى ≤2 أو ذاكرة ≤2GB): بلا حلقة حية إطلاقًا — التقاط يدوي بأفضل إطار فقط؛ الجوال/المكتبي: حلقة 500ms محافظة (قبل أي قياس ميداني) |
| ب5) البوابات لا تُمس | ✅ مكتمل | كل إطار يمر عبر OcrModule.recognize() نفسه (لاتيني 60%، ثقة 45، إعفاء CAS الصالح)؛ لا محرك جديد في scan-live.js |
| ج) حماية القراءة الجارية | ✅ مكتمل | لا مغادرة لشاشة المسح أثناء عمل المحرك (التنقل يعود لـ #/scan)؛ حر تمامًا فور الانتهاء؛ بلا أي تغيير في التصميم القائم — تلبية لبروتوكول المراجعة الميدانية §ج1 |
| إصلاحات دمج الجولة السابقة | ✅ مكتمل | showPreview كان يستقبل canvas (createObjectURL يفشل) → showPreviewCanvas؛ مستمعان متضاربان على زر الكاميرا (فتح حوار + بث معًا) → مسار حي واحد؛ أزرار التصوير لا تُستعاد بعد الإيقاف → أُعيدت؛ الحلقة الحية لم تتوقف عند النجاح → توقفت؛ لا إلغاء للقراءة الفعالة عند بدء البث → يُلغى؛ نص عربي ثابت خارج t() → مفاتيح قواميس |
| الاختبارات | ✅ كلها خضراء | ثابت 134/134 (بينها stage-b الجديد 35 فحصًا)؛ متصفح حقيقي: stage-b-browser 12/12 (بث getUserMedia فعلي، فحص رخيص مقيس، ج بمسح OCR حقيقي كامل) + ui-browser 11/11 |
| حدود قياس هذه الجولة | 📌 صادق | لم تُقَس: كروم 109 فعليًا (فحص ثابت فقط)، أزمنة القراءة الحية على هاتف حقيقي (الحلقة 500ms افتراض محافظ بانتظار القياس الميداني) |
| النشر | ✅ مكتمل | إيداعان تقدمان دُفعا إلى main: إيداع الكود (ب+ج معًا — الملفات مشتركة بعمق، سابقة A9) ثم إيداع الوثائق؛ SW v19؛ إصدار 1.5.0؛ التحقق الحي من الرابط أدناه |
| استعادة مجموعة E2E العشرين (جولة لاحقة) | ✅ مكتمل | ملاحظة «لم تصل مساحة العمل» كانت قديمة: الملفان tests/v2-suite.html وtests/v2-run.mjs وجدا سليمين — حُدِّثا للخط الحي الحالي (قواعد البيانات الخمس + بوابة الثقة 45 + إعفاء CAS المضيَّق، دعم BASE_URL للمنفّذ) وأُعيد تشغيلها بمتصفح حقيقي: **20/20 PASS** محليًا (تشغيلة واحدة متصلة)؛ محاولة التشغيل على الرابط المنشور تجاوزت حد زمن المنصة (تنزيل أصول OCR ~15MB عبر الشبكة) فوثِّق بدل إجباره |

## جولة 2026-09-25 — المرحلة أ: تشخيص بطء القراءة الثابتة وعلاجه بالقياس

| البند | الحالة | الدليل |
|---|---|---|
| صفر) شبكة الأمان | ✅ مكتمل | مجموعة E2E العشرين موجودة وتعمل 20/20 (الإيداع 37401c2 قبل أي تعديل) |
| أ1) وحدة التشخيص | ✅ مكتمل | src/ocr-diagnostics.js (مستمية في الإنتاج؛ تفعيل صريح فقط) + ربط 4 نقاط في حلقة السلّم بحارس lazy (صفر كلفة بلا تفعيل) + tests/ocr-diag-suite.html + tests/ocr-diag-run.mjs |
| أ2) القياس الفعلي | ✅ مكتمل | 37 صورة (20 اصطناعية + 17 حقيقية) بمتصفح Chromium حقيقي؛ الجدول الكامل والحكم في docs/ocr-speed-diagnosis.md؛ السجلات الخام /tmp/ocr-diag-all.json |
| أ3) الإصلاح بالأدلة | ✅ مكتمل (فرع 1 فقط) | الدليل: 5 صور (rotated_90 و101/8/9/images.jpg) فيها محاولة dbBest=100 رفضها مخرج البوابة النهائي — أُدخل قفل مبكر (earlyLock) في src/ocr.js عند exactHit أو CAS صالح؛ البوابتان باقيتان بلا إلغاء والمطابقة لم تُمس؛ انحراف موثق عن return acceptResult الحرفي في docs/ocr-speed-diagnosis.md |
| أ4) إعادة القياس | ✅ مكتمل | نفس الصور بالضبط: 4 صور مرفوضة كليًا أصبحت مقبولة بقبول مرتبط بمحاولة dbBest=100 (Isoprothiolane/Spinosad/Soap/Oxadiazon)؛ صفر قبول جديد بغير تأكيد؛ E2E 20/20 بعد الإصلاح |
| اختبار جديد | ✅ مكتمل | tests/ocr-early-confirm.test.mjs ‏12/12 على القواعد الخمس الحقيقية |
| الاختبارات الكاملة | ✅ كلها خضراء | ثابت: verify 120 + early-confirm 12 + stage-a 25 + stage-b 35 + eng-only/latin/conf-gate + data-health + status-legend + no-hardcoded-arabic؛ متصفح وE2E بعد الإصلاح: 20/20 |
| رفع الكاش | ✅ مكتمل | SW v19→v20 بترويسة مطابقة في نفس الإيداع |
| حدود الجولة (صادق) | 📌 | صور الاستنفاد بلا تأكيد (7.jpg 111ث، level3_damaged 104ث) لم تُعالج — خارج نطاق البوابات؛ documented كأولوية تالية؛ ground-truth.csv ما زال قالبًا فارغًا؛ لم يُقس على كروم 109 فعليًا |

## جولة 2026-09-25 — المرحلة ب: مسح سجل البحث (تشخيص ثم إحكام)

| البند | الحالة | الدليل |
|---|---|---|
| ب) التشخيص الحقيقي أولًا | ✅ مكتمل | فحص src/app.js: المسار الحالي كان **صحيح الشكل** (idbClear ثم openHistory) عكس توقع البرومبت — الإشكال المرصود ميدانيًا لم يَعُد يُعاد إنتاجه؛ اختبار متصفح حقيقي (tests/history-clear.test.mjs): بذر 2 سجل في IndexedDB فعلي → إعادة تحميل كاملة (يبقيان) → نقر زر المسح الفعلي → المخزن فارغ مباشرة (getAllKeys=[]) → إعادة تحميل كاملة (لا عودة) 6/6 PASS |
| ب) الإحكام المطبَّق | ✅ مكتمل | ترتيب البرومبت صار صريحًا ومحصّنًا: openHistory يعيد Promise، والعرض يُبنى من قراءة جديدة للمخزن دائمًا؛ إشعار بالنتيجة في كل المسارات (نجاح/فشل) داخل قائمة السجل بنمط .notice — لا فشل صامت |
| قواميس الإشعار | ✅ مكتمل | history.cleared/history.clearFail في القواميس الأربعة (ar/en/fr/zh) — لا نص عربي ثابت خارجها (no-hardcoded-arabic PASS) |
| الاختبارات | ✅ خضراء | history-clear 6/6 + stage-b (فحص إحكام المسح جديد) + verify + no-hardcoded-arabic + status-legend |
| رفع الكاش | ✅ مكتمل | SW v20→v21 بترويسة مطابقة في نفس الإيداع |


| المرحلة | الحالة | الإيداع | الدليل |
|---|---|---|---|
| بوابة الأسئلة (6 بنود) | ✅ أُجيبت 2026-09-20 | — | docs/DECISIONS.md |
| أ) الحماية والتوحيد | ✅ مكتمل | b754f4c | حزمة backup-2026-09-20.bundle + وسوم أرشيف ×3 مدفوعة + نقل الشيفرة الميتة archive/ + README |
| ب) سلامة القرار والمطابقة | ✅ مكتمل | cf2ff79 | src/cas.js + namepairs 19/19 + nearpairs 400/400 + 13 خطأ CAS موثّق لا يُصحّح |
| ج) البيانات والمصادر | ✅ مكتمل | cf2ff79 | docs/data-provenance.md + data-health 18/18 + FAO معطّل بعلم موثّق |
| د) التعرف البصري | 🔄 د2–د5 منجزة · د1 (الخط الأساسي) متوقف على الصور · د3 (محرك بديل) لاحقًا | — | probeImage + تأكيد + إلغاء + تشخيص محلي + واجهة scan() الموحدة |
| هـ) تعدد اللغات | ✅ مكتمل | 051e9bc | رسائل OCR انتقلت للقواميس + اختبار النص العربي الثابت 2/2 + dir/lang حيوي |
| و) التراخيص والخصوصية والدعم | ✅ مكتمل | (إيداع و) | tesseract.min.js.LICENSE.txt مستعاد + THIRD_PARTY.md + about/privacy + زر دعم من config/support.json |
| ز) النشر للمراجعة الميدانية | ✅ مكتمل | (إيداع ز) | SW v7 + precache الجديد + تحقق E2E بعد إصلاح انحدار status() |
| انحدار E2E (اكتُشف أثناء ز) | ✅ مُصلَّح | 66f7087 | انهيار المكدس في status()↔ensureWorker — حارس إعادة دخول في src/ocr.js؛ E2E 20/20 |
| ز1) توافق المسار الفرعي | ✅ مكتمل | cfc7f24 | worker/core/lang عبر appRoot() بدل الجذر الأصلي؛ static 99/99؛ E2E 20/20 |
## جولة 2026-09-25 — المرحلة أ: مسح النتائج القديمة + زر إزالة الصورة + الأتمتة الكاملة

| البند | الحالة | الدليل |
|---|---|---|
| أ1) مسح فوري للنتائج القديمة | ✅ مكتمل | كتابة/حذف نص البحث → `clearSearchResults()` فورًا؛ صورة/مسح جديد → `scanSeq++` + `clearScanResults()` قبل أي معالجة؛ المسح المكبوت لا يرسم شيئًا (حارس التوليد بعد recognize وفي catch)؛ اختبار ثابت `tests/stage-a.test.mjs` 24/24 |
| أ2) زر إزالة/تبديل الصورة | ✅ مكتمل | `#cancelImageBtn` عائم فوق المعاينة (44px، i18n ×4): إلغاء المسح الجاري، سحب blob، تفريغ مدخلي الملفات، عودة لحالة الاستعداد بلا reload |
| أ3) الأتمتة الكاملة | ✅ مكتمل | لوحة التأكيد حُذفت؛ النص الناجح يُمرَّر تلقائيًا لمحرك البحث الحالي وتُعرض النتائج داخل `#scanResults` بنفس عارض القرار؛ الرقاقات كإعادة بحث يدوية اختيارية عبر `#ocrText`؛ بوابات ocr.js (لاتيني 60%، ثقة 45، إعفاء CAS الصالح) لم تُمس |
| الاختبارات | ✅ كلها خضراء | مرحلة أ 24/24؛ ثابت: ui-icons 6 + no-hardcoded-arabic 2 + data-health 23 + judge 38 + namepairs 17 + nearpairs 400 + ocr-eng-only 11 + latin 21 + conf 26 + verify 117؛ متصفح 11/11؛ E2E حقيقي 20/20 |
| النشر | ✅ مكتمل | إيداع `77a08bb` دُفع f64816b..77a08bb؛ SW v18؛ إصدار 1.4.2؛ التحقق الحي من الرابط أدناه |

| ز5) التحقق دون اتصال (محلي) | ✅ بالتشغيل | — | SW نشط؛ القواعد 77/411/1483/2199؛ بحث Glyphosate 100% بلا شبكة؛ 404 الوحيد = fao.json (معطّل عمدًا) |
| ز4) نشر GitHub Pages | ⏳ خطوة يدوية واحدة | — | واجهة API للنشر محجوبة (403): Pages → Source → main /(root) من إعدادات المستودع |
| إقفال الجولة: اختبارات نهائية + دفع main | ✅ مكتمل | 4267d00 | ثابت 557/557 (verify 99 + data-health 18 + judge 38 + nearpairs 400 + i18n 2) + E2E V2 20/20؛ دُفع 3a05852..4267d00 إلى origin/main |
| واجهة: مطابقة تصميم المرجع | ✅ مكتمل | 74beb48 + bb6dbbf | 6 عروض hash + سمتان + خط Plex OFL + أيقونات Lucide ISC؛ ثابت 110+6+7، E2E 20/20، لقطات 13؛ دُفع وتحقق من الرابط المنشور: version.json 1.3.0، woff2/icons/icons.js ‏200، sw v8 مسجّل |
| مؤجَّل: docs/AGENTS.md | ✅ مكتمل | (إيداع و) | وثيقة صلاحيات الوكلاء 0/1/2 بلا أي وكيل خلفي |

## جولة 2026-09-23 — قاعدة أمريكا الماستر (المرحلة ب) + حد الثقة (أ4)

| البند | الحالة | الدليل |
|---|---|---|
| ب) إعادة بناء EPA من الدفتر الرسمي | ✅ مكتمل | `tools/build-epa-master.js` يقرأ `sources/EPA_Master/EPA_Master.xlsx` ويؤكد أرقام التحقق الخمسة قبل الكتابة؛ `epa.json` = 1,361 (النشط) و`epa-cancelled.json` = 1,425 (الملغى) بحقل `row` تسلسلي و`status_raw` عربي حرفي؛ data-health 23/23؛ واجهة (بطاقة/قاعدة/رتبة/تحكيم/قواميس ×4) مكتملة؛ SW v15/v16 يجهّزان القاعدة الخامسة |
| أ4) حد الثقة للقراءة غير المهيكلة | ✅ مكتمل | `MIN_CONFIDENCE = 45` في مسار OCR الحي؛ بروب حي: الضجيج (ثقة 24) يُرفض `ocr.rejected.conf` والملصق (90) يُقبل؛ اختبار ثابت 26/26؛ الإعفاء مُضيَّق (قرار المستخدم) حصرًا لرقم CAS الصالح فحص التحقق؛ SW v16→v17 |
| أ2/أ3) أداة المقارنة + التوصية | ✅ موثقة | `tests/ocr-compare.mjs` (أداة تطوير فقط)؛ قياس حي للعينة الكاملة: 0 قبول خاطئ، 0 رفض خاطئ؛ لا مفاتيح وصول؛ PP-OCRv5 غير مقيس (لا تنزيلات وقت الجلسة)؛ التوصية: **لا تبديل محرك** — بانتظار قرار المستخدم في `docs/ocr-engine-comparison.md` |
| إصلاح انحدار E2E المكتشف | ✅ مُصلَّح | حالات cas_* الأربع (صورة CAS فقط ≈52–58% لاتيني) كانت تُرفض بالبوابة اللاتينية من الجولة السابقة → رقم CAS الصالح فحص التحقق يُعفى من البوابتين؛ E2E 20/20 بعد الإصلاح |
| المرحلة ج) إصلاح زر المشاركة | ✅ مكتمل (جولة المتابعة) | الزر كان صامتًا بلا Web Share (فايرفوكس/كروم ديسكتوب): الآن Web Share → نسخ للحافظة (isSecureContext) → بطاقة vCard بالرابط، مع إشعار بالنتيجة عبر `share.copied/saved/fail` ×4 قواميس؛ SW v17 |
| الاختبارات النهائية | ✅ كلها خضراء | ثابت 539 فحصًا (9 ملفات) + verify 115/115 + متصفح 11/11 + E2E 20/20 |

## ملاحظات التشغيل (حدود المنصة المطبقة)

- الأوامر تُقتل بعد ~30–180 ث؛ الذاكرة 2 غيغابايت؛ لا عمليات خلفية طويلة → كل خطوة طويلة تُقسّم.
- الاختبار الثابت: `node tests/verify.mjs` — الوظيفي: جناح الـ20 حالة عبر متصفح حقيقي في `/tmp/e2e` (خارج المستودع).

### تعديل صورة المطور — 2026-09-21
- طلب المستخدم: العرض الدائري الأنيق. نصف القطر 50% + حلقة تحديد رقيقة عبر ظل مزدوج. بلا أي تغيير آخر (الكاش v10، الإصدار 1.3.2). **ملف `assets/developer.jpg` لم يصل مساحة العمل بعد** — عند إضافته يظهر تلقائيًا بشكل دائري (الكود يقرأ المسار ويخفي البديل عند نجاح التحميل).

### تسليم جولة الواجهة — 2026-09-21
- إيداعان تقدميان إلى main: `74beb48` (التنسيق + الأصول + الاختبارات) ثم `bb6dbbf` (سكربتا اللقطات والفحص).
- تحقق من الرابط المنشور (بالتشغيل): version.json = 1.3.0/released 2026-09-21، كل الأصول الجديدة 200، index.html المنشور يطابق المحلي (data-view + الخط + التنقل)، sw.js = mustashar-v8 وserviceWorker.register موجود.
- لم يُقَس: كروم 109 فعليًا (فحص ثابت فقط) — اختبار يدوي على جهاز المستخدم.
- الملاحظات النصية المؤجلة مسجلة في DECISIONS (قسم جولة الواجهة) وتُعالج في جولة مستقلة.
